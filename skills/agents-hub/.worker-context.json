{
  "provider": "agents-hub",
  "version": "1.1",
  "context": {
    "symlinks": [],
    "env": {},
    "prompt_sections": {
      "hub_communication": "## Hub Communication\n\nLoad the agents-hub skill for inter-agent coordination. The hub database is stored in `.git/devpartner/hub.db` (shared across worktrees). Use `hub exec` commands to post findings, search context, and coordinate with other agents."
    }
  }
}
