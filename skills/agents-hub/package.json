{
  "name": "@agent-collab/hub",
  "version": "0.2.0",
  "description": "Agent Communication Hub - Real-time messaging and knowledge sharing for multi-agent systems",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "ncc build src/skill-cli.ts -o scripts --minify --no-source-map-register && node -e \"const fs=require('fs');const walk=(d)=>{for(const e of fs.readdirSync(d,{withFileTypes:true})){const p=d+'/'+e.name;if(e.isDirectory()){walk(p);if(fs.readdirSync(p).length===0)fs.rmdirSync(p);}else if(p.endsWith('.d.ts')||p.endsWith('.d.ts.map')){fs.rmSync(p,{force:true});}}};if(fs.existsSync('scripts'))walk('scripts');\"",
    "dev": "tsc --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "node -e \"const fs=require('fs');for(const p of ['dist','scripts/index.js','scripts/build'])fs.rmSync(p,{recursive:true,force:true});\"",
    "publish:skill": "node publish-skill.mjs"
  },
  "dependencies": {
    "better-sqlite3": "^11.7.0",
    "commander": "^13.1.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.12",
    "@types/node": "^22.10.5",
    "@vercel/ncc": "^0.38.4",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "license": "MIT"
}
