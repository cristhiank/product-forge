{
  "name": "@copilot-skills/backlog",
  "version": "1.2.0",
  "description": "Backlog Agent Skill - CLI for Kanban-lite backlog maintenance",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "backlog": "./scripts/index.js"
  },
  "scripts": {
    "build": "ncc build src/skill-cli.ts -o scripts --minify --no-source-map-register && find scripts -name '*.d.ts' -o -name '*.d.ts.map' | xargs rm -f 2>/dev/null; find scripts -type d -empty -delete 2>/dev/null; true",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rm -rf dist scripts/index.js",
    "publish:skill": "bash publish-skill.sh"
  },
  "dependencies": {
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "@vercel/ncc": "^0.38.4",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "files": [
    "dist",
    "scripts",
    "references",
    "SKILL.md",
    "README.md"
  ],
  "license": "MIT"
}
